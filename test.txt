// => sa
/* ESTE ES UN COMENTARIO NORMAL */
/* este es/* un comentario */
anidado */

let age = 1;

let name = "Monkey";

let result = 10 * (20 / 2);

let myArray = [1, 2, 3, 4, 5];

let thorsten = {"name": "Javier", 28: "age"};

myArray[0] // => 1

thorsten["name"] // => "Thorsten"

let add = fn(a, b) { return a + b; };

let fibonacci = fn(x) {
    if (x == 0) {
        let inside = 10;
    } else {
        if (x == 1) {
            let var2 = "Hola";
        } else {
            fibonacci(x - 1) + fibonacci(x - 2);
        }
    }
};

